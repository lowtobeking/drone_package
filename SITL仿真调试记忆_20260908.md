# SITL 仿真 Bring-up 调试记忆（2026-09-08）

> 用途：本次把「本机 WSL2 上 PX4+gz 多机 SITL + MPC」从“boot 卡死 / 无 GPS / MPC 收不到状态”
> 一路修到“单机、pair2 双机干净 Ready + MPC 起飞悬停”。下次对话先读本文件即可快速恢复上下文，
> 不必重跑全部排查。

---

## 0. 一句话结论
本机仿真最终 **统一跑 PX4-1.16 树**（不是 $HOME/PX4-Autopilot 的 main）+ 自建“全系统 world”，
配 px4_msgs @ v1.16.2。入口脚本已可复现：单机 headless / 单机 GUI / pair2 GUI 均通过。

## 1. 环境事实（别改错对象）
- WSL2 Ubuntu 24.04，**4 核 / ~7.8 GB**，`DISPLAY=:0`(WSLg)，`/mnt/wslg` 存在 → Gazebo GUI 能显示到 Windows。
- ROS：`/opt/ros/jazzy`，RMW=fastrtps，`ROS_DOMAIN_ID=0`。
- Gazebo：Harmonic `gz sim 8.13`（系统），`gz sim -s`=无头 server，加 GUI 去掉 `-s` 即可。
- PX4 树与产物：
  - **PX4-1.16（本项目实际用）**：`drone_package_20260908/PX4-Autopilot-1.16`
    - SITL 二进制：`.../PX4-Autopilot-1.16/build/px4_sitl_default/bin/px4`
    - 消息：`msg/versioned/*.msg`（DDS 话题带版本后缀，如 vehicle_status_v1）
  - main（已弃用，勿默认用）：`$HOME/PX4-Autopilot`；其 x500 模型 imu 无噪声→STALE，gz_bridge 无 navsat 消费→无 GPS。
- ROS 侧消息包：`ros2_ws/src/px4_msgs` 现在 **detached 在 tag v1.16.2**（必须与固件 1.16 匹配，否则话题“静默不可见”）。

## 2. 四个根因链（现象→修法）——这是本次的核心记忆

### A) `Gyro #0 fail: STALE!` / `Sensor missing`（boot 卡死、永不许解锁）
- 现象：PX4 main spawn 的 `x500` 外层模型 `imu_sensor` **无噪声**；静止时陀螺输出≈恒 0
  （抖动 <1e-6）→ 被 `DataValidator` 判 STALE → 无 IMU → `arming denied`。
  （px4 1.16 的 `x500` 用 `<include merge='true'> model://x500_base`，自带 imu 噪声 + navsat + 磁罗盘，无此问题。）
- 验证实验（以后可复用）：同一 world 里 box(有噪声,std≈5e-3) vs x500 外层(≈1e-17)；
  `x500_base` 直连(≈1.8e-4 有噪声) vs 外层 `x500`(无噪声) → 实锤。
- 修法：**弃用 main、项目统一切 PX4-1.16**；无需改模型。

### B) 1.16 裸 world 没有传感器系统插件 → 传感器根本不发布
- 现象：1.16 `Tools/.../worlds/default.sdf` 不含 `gz-sim-sensors-system / Imu / NavSat …`（主代码 main 的 default.sdf 自带）。
- 修法：自建**全系统 world** `gz_overrides/worlds/default.sdf`，含：
  `Physics/UserCommands/SceneBroadcaster/Contact/Imu/AirPressure/NavSat/Magnetometer/Sensors(ogre2)` +
  `<spherical_coordinates>` + 可选 `<gui>`(3D View/CameraTracking/WorldControl/Stats)。
  启动 gz 一律用 `$SITL_WORLD`，别直接用 PX4 自带 default.sdf。

### C) GPS 水平定位永远无效（`xy_valid=false`、无 home / 不 Ready）
两个必要条件缺一不可：
1. **NavSat 数据要有内容**：gz-sim `NavSat` world 插件 + world 里必须配 `<spherical_coordinates>`，
   否则 navsat_sensor 发布空消息（无 lat/lon）。
2. **EKF 需要航向基准**：原 4001 rc `SYS_HAS_MAG 0` → 静态机无磁航向 → EKF 拒绝 gnss pos 融合 → xy 永 false。
   改 `SYS_HAS_MAG 1`（配合 world 的 `gz-sim-magnetometer-system` + x500_base 自带 mag sensor）。
- 提醒：PX4 **main** 的 gz_bridge 根本不消费 navsat（全树 grep 只有参数/server.config），所以 GPS 在 main 上无解——
  这也是“换 1.16”的决定性理由。1.16 的 `GZBridge::navSatCallback` 直接 navsat→`sensor_gps`。

### D) MPC “无限 retry ARM+OFFBOARD”，但飞机其实在飞
- 现象：drone 实际已 Armed/Takeoff，MPC 仍 `retry ARM (nav=0,arm=0)`；`ros2 topic echo vehicle_status_v1` 为空。
- 根因：**ROS 侧 px4_msgs 是 main 的消息集**，与固件 1.16 versioned 消息结构不一致（VehicleStatus main=95 字段 vs 1.16=105 字段）
  → Fast-DDS 类型不匹配 → 该话题在 ROS 侧“静默不可见”（不报错、收不到）。
- 修法：`ros2_ws/src/px4_msgs` checkout **v1.16.2**（`VehicleStatus` 与固件 1.16 逐字段 105/105 全等），
  `colcon build --packages-select px4_msgs`（~1m14s）重建。

### E) 话题名随固件版本化（mpc/leader 订阅选择）
- main：`vehicle_status_v4`、`vehicle_local_position_v1`
- **1.16**：`vehicle_status_v1`、`vehicle_local_position`（无后缀）
- 修法：`mpc_node.py`/`leader_node.py` 增加按 `px4_version` 选话题；`scenarios.yaml defaults` 增 `px4_version: '1.16'`；
  `swarm_launch.py` 把 `px4_version` 也透传给 leader。

---

## 3. 改动清单（都已落地，未提交 git）
| 文件 | 改动 |
|---|---|
| `run_env.sh` | `PX4_DIR` 默认=`BASE/PX4-Autopilot-1.16`；导出 `SITL_WORLD=$BASE/gz_overrides/worlds/default.sdf`；GZ_SIM_RESOURCE_PATH=该 PX4 树 |
| `gz_overrides/worlds/default.sdf` | 全系统 world + 球坐标 + GUI（新增文件） |
| `PX4-Autopilot-1.16/.../airframes/4001_gz_x500` | `SYS_HAS_MAG 0→1`（源文件 + `build/px4_sitl_default/etc/.../4001_gz_x500` 副本同步） |
| `Multi-UAV-simulation-full/mpc_control/mpc_node.py` | `local_position_topic` 按 `px4_version` 选（main→`_v1`，1.16/1.14→无后缀） |
| `Multi-UAV-simulation-full/mpc_control/leader_node.py` | 新增 `px4_version` 参数，按固件选 drone0 local topic |
| `Multi-UAV-simulation-full/launch/swarm_launch.py` | leader 参数透传 `px4_version` |
| `Multi-UAV-simulation-full/config/scenarios.yaml` | `defaults.px4_version: '1.16'` |
| `Multi-UAV-simulation-full/tools/spawn_px4.sh` | 仅在存在 `gz_overrides/models` 时前置 override 模型路径 |
| `ros2_ws/src/px4_msgs` | checkout `v1.16.2`（detached）；已 `colcon build` |
| `sitl_solo_check.sh` / `sitl_solo_gui.sh` / `sitl_pair_gui.sh` | 复现入口（BASE 根下，新增） |

acados 缓存：`~/.cache/mpc_control/acados_di_mpc_*`（现已有 `lag0p500sb_v0_m1`、pair2 用到的 `v1_m1` 也已现场编译缓存）。

## 4. 复现命令（下次直接跑）
先 source 环境（脚本内部已 source）：
```bash
BASE=~/drone_package_20260908
source $BASE/run_env.sh     # 会打印 PX4_DIR / SITL_WORLD，确认指向 1.16 与 override world
```
- 单机无 GUI 验收（Ready+MPC 起飞悬停）：
  ```bash
  bash $BASE/sitl_solo_check.sh
  ```
- 单机 GUI（看窗口在 Windows 桌面）：
  ```bash
  bash $BASE/sitl_solo_gui.sh 45      # 45=悬停保持秒
  ```
- **pair2 双机 GUI**：
  ```bash
  bash $BASE/sitl_pair_gui.sh 60
  ```
- 若要手动分步（与脚本等价）：
  ```bash
  pkill -9 -f 'px4 -d -i'; pkill -9 -f 'gz sim'; pkill -9 -f MicroXRCEAgent; pkill -9 -f 'swarm_launch|mpc_node|leader_node'
  rm -f /dev/shm/fastrtps_*
  gz sim -r "$SITL_WORLD" &          # 无 GUI 用 gz sim -s -r
  sleep 15; MicroXRCEAgent udp4 -p 8888 &; sleep 3
  cd "$PX4_DIR"
  PX4_DIR=$PX4_DIR FORMATION=pair2 bash $BASE/Multi-UAV-simulation-full/tools/spawn_px4.sh
  # 等日志: grep 'Ready for takeoff' ~/px4_logs/px4_{0,1}.log
  ros2 launch mpc_control swarm_launch.py scenario:=S2_pair2_hover
  ```
- 顶部清理一键（每个脚本内部都有，手动打断后用它）：
  ```bash
  pkill -9 -f 'px4 -d -i'; pkill -9 -f 'gz sim'; pkill -9 -f MicroXRCEAgent; pkill -9 -f 'swarm_launch|mpc_node|leader_node'
  ```

## 5. 实测结果（可直接引用）
- 单机/双机均：**Ready for takeoff ~5s**，`xy_valid/v_xy_valid=true`，GPS fix3/eph0.9，磁航向正常。
- px4 事件：`Armed by external command` → `Takeoff detected`。
- MPC（S0_solo1_hover / S2_pair2_hover）：两机 `acados OCP ready` → `[dN] 已离地（上升 ~0.67m 持续爬升 1.0s）`；
  悬停 z≈**-5.0m**（d1 ≈ -5.05m），v≈0，队形间隔 3m。MPC 不再无限 retry（能读到 arm=2 / nav=14）。

## 6. 已知遗留 / 下次续接点
1. **drone1（2 号机）健康告警**（不影响已飞行）：`Preflight Fail: No valid data from Compass 1` + `Battery unhealthy`
   —— 疑似 SITL 供电/磁罗盘数据晚到。若要长时间多机或消告警，优先查：instance1 mag 桥接、`SIM_BAT_*` 与 4001 rc 的 `CBRK_SUPPLY_CHK`/`SIM_BAT_MIN_PCT`。
2. **px4_msgs 锁定 v1.16.2**：若哪天切回 PX4 main 跑，必须把 `ros2_ws/src/px4_msgs` 切回 main 并 `colcon build`，且话题名/`px4_version` 全要跟着变（见 §2E）。
3. 若某话题 ROS echo 为空且不报错 → 优先怀疑“固件与 px4_msgs 消息结构不匹配”（DDS 类型静默不匹配），不是 Qos/命名问题。
4. GUI 若打不开：确认 `DISPLAY=:0`、`/mnt/wslg`；渲染兜底 `LIBGL_ALWAYS_SOFTWARE=1`（脚本已默认设）。
5. 其余队形的实跑进度见下方 §8（2026-09-09）：**trio3 已实跑通过**（headless+GUI，等边~5.2m）；
   仅 cross5/star5(5架)、grid9(9架) 及以上仍未跑——4 核 WSL 下 5 架会吃 CPU/掉帧，grid9 基本跑不动。
   跑法一致：`FORMATION=...` + `swarm_launch.py scenario:=S*`，新 drone_id×邻居组合首次会现场编 acados（分钟级）。
6. `drone_package_20260908` 顶层非 git；改动均未提交（如需提交，mpc 三文件 + scenarios + launch 在 `Multi-UAV-simulation-full` 仓库内）。

## 7. 调试速查
- px4 日志：`~/px4_logs/px4_{0,1,...}.log`；Ready 判据 `grep 'Ready for takeoff'`；STALE 判据 `grep 'Gyro #0 fail'`。
- ulog 落在各实例 cwd：`<PX4树>/build/px4_sitl_default/rootfs/<i>/log/YYYY-MM-DD/*.ulg`（可用 pyulog 读）。
- 看 sensor_gps/estimator 内部状态用 ulog 最可靠（`estimator_status`、`estimator_aid_src_gnss_pos.fused/time_last_fuse`、`sensor_gps.fix_type`）。
- ROS 观测：
  - drone0 无命名空间：`/fmu/out/vehicle_status_v1`、`/fmu/out/vehicle_local_position`
  - drone≥1：`/px4_<i>/fmu/out/...`
  - echo：`ros2 topic echo /fmu/out/vehicle_status_v1 --qos-reliability best_effort --once`
- gz 侧确认传感器在发：`gz topic -l`；raw：`gz topic -e -t /world/default/model/x500_0/link/base_link/sensor/navsat_sensor/navsat -n 3`

---

## 8. 续：2026-09-09 三级(headless)+GUI 全真实验收结果

> 上一会话只实跑了 solo1/pair2；本会话把「能否从开包直接复现」彻底验证到 trio3，
> 并修掉一个让 `sitl_solo_check.sh` 一启动就崩的 bug。以下为可复现结论，下次无需重跑。

### 8.1 修掉的一个"复现入口即崩"bug（重要）
- 现象：`bash sitl_solo_check.sh` 秒退，报 `AMENT_TRACE_SETUP_FILES: unbound variable`（随后还有
  `AMENT_PYTHON_EXECUTABLE`、`COLCON_*`、`ROS_*` 等一串）。
- 根因：该脚本是 `set -u`（严格未设即报错）；而 ROS jazzy 的 `setup.bash`/各 `local_setup.*` 在 source 时
  会读取一堆缺省未设变量 → `set -u` 下直接中断。
- 修法：`run_env.sh` 在 source `/opt/ros/jazzy/setup.bash` 与本地 `ros2_ws/install/setup.bash` 期间
  **临时 `set +u`**，结束时恢复原 `-u` 态：
  ```bash
  _u_flag="$(case "$-" in *u*) echo u;; *) echo '';; esac)"
  set +u
  source /opt/ros/jazzy/setup.bash
  # ... source ros2_ws ...
  if [ "$_u_flag" = "u" ]; then set -u; fi
  ```
  三个入口脚本与用户手工 `source run_env.sh` 都受益，`-u` 的严格校验不丢。

### 8.2 真机级裸跑结论（本机，4核/7.8GB，均退出码 0）
在本包内（PX4-1.16 + 自建 world + px4_msgs v1.16.2，改动见 §3，未变）直接跑，**无需任何额外配置**：

| 档位 | headless 入口 | GUI 入口 | 结果 |
|---|---|---|---|
| solo1（1机） | `sitl_solo_check.sh` | `sitl_solo_gui.sh [s]` | ✅ Ready~5s，MPC 悬停 z≈-5.0m，v≈0，Armed→Takeoff |
| pair2（2机） | `sitl_pair_check.sh`（本次新增） | `sitl_pair_gui.sh [s]` | ✅ 双机 Ready~5s，各 hold z=-5m，间隔 3m（出生 0/-3） |
| trio3（3机） | `sitl_trio3_check.sh`（本次新增） | `launch_trio3_gui.sh`（本次新增） | ✅ 三机 Ready~5s，等边三角形边长≈5.2m，高~5m |

- 三机 trio3 每机 2 邻居（m2）OCP **首次现场编 acados 未拖后腿**：t+45s 内三台已 `acados OCP ready` 且全 `已离地`。
- 新增 headless 脚本（行为=对应 GUI 的 headless 版，避免起窗口，便于自动验收）：
  `sitl_pair_check.sh`、`sitl_trio3_check.sh`。solo 用原 `sitl_solo_check.sh`。
- trio3 GUI 封装：`launch_trio3_gui.sh`（前台版本即可看窗口；本包未提供原生 trio3 GUI 脚本）。
- 实测 px4 事件链三机一致：`Ready for takeoff`→`Armed by external command`→`Takeoff detected`；
  悬停期 `arming_state=2 / nav_state=14`（armed + offboard 接管），MPC 持续 `solve status=0`。

### 8.3 GUI 可视化观看要点（Windows 桌面）
- 单机/双机/三机 GUI 均能经 WSLg 出窗口。自动验收时若后台跑，会让 gz GUI 一同活到脚本 clean；
  需要长时间观看就把 keep（首参，秒）调大：
  - `bash sitl_solo_gui.sh 120`；`bash sitl_pair_gui.sh 120`；`NOHUP bash launch_trio3_gui.sh 2>&1 &`（其内部 KEEP 写在脚本里）。
- 三机窗口中三架 x500 会各就等边顶点悬浮；软件渲染（LIBGL_ALWAYS_SOFTWARE=1）4 核仍流畅。

### 8.4 尚未实跑 / 下次续接
1. **cross5 / star5(5架)、grid9(9架)及更大** 仍未实跑。前文 §6.5 提醒仍然有效：
   `formation:=cross5` + `swarm_launch.py scenario:=S6_cross5_hover` 可试，但 4 核下 5 架吃 CPU/掉帧、
   grid9 基本跑不动；且多邻居(d>1)首架 OCP 依赖尚未生成缓存，会现场编多次。若确要验证，建议单开一台
   更强主机，而非在本 4 核 WSL 上。
2. 若某天切回 PX4 **main** 跑，需把 `ros2_ws/src/px4_msgs` 切回 main 并 `colcon build`，话题名与
   `px4_version` 全变（见 §2E/§6.2）。
3. `drone_package_20260908` 顶层仍非 git；本次改的只有 `run_env.sh` 与新增的 3 个 `sitl_*_check.sh`、
   1 个 `launch_trio3_gui.sh`（均在顶层，非 git）。


---

## 9. git / 仓库：代码已收编并推到 GitHub（2026-09-09）

### 9.1 仓库
- 远程：`git@github.com:lowtobeking/drone_package.git`（SSH）
- 本地仓库根：`/home/caolihao/drone_package`（=本 md 所在目录）
- 这是**工程仓库**，只收工程代码/编排/自建world/文档，**不含固件与第三方依赖**。
- 本 md 的「正本」在仓库里（`/home/caolihao/drone_package/SITL仿真调试记忆_20260908.md`）；
  源包副本 `/home/caolihao/drone_package_20260908/...` 已同步一致。

### 9.2 收录结构（提交内容）
- `Multi-UAV-simulation-full/`：MPC 完整业务工程（mpc_control/launch/config/tools/deploy/docs）+
  `report/` 仅留文档（42 篇 md）；已去除 .git / .bak* / 论文图/csv / simulink 数据附件。
- 顶层编排：`run_env.sh`、`sitl_{solo,pair,trio3}_check.sh`、`sitl_*_gui.sh`、`launch_trio3_gui.sh`、
  `gz_overrides/worlds/`、`mesh_dds.*` 等脚本/配置；本 md；`README.md`。
- 排除：PX4(-1.16/PX4-Autopilot)、handoff、flights、Documents、acados、ros2_ws(_real)、
  Micro-XRCE、*tar.gz、*.bak*、论文/图/csv 等。均放在 `.gitignore`。
- 依赖（PX4、gz、px4_msgs@v1.16.2、acados、XRCE agent）由外部按 §7/README 提供，不在此仓库。

### 9.3 历史与复现（跑工程前先 `source run_env.sh`，见 §4）
- 仿真验证结论见 §8（三级 headless+GUI 通过：solo/pair/trio3）。
- **注意**：仓库这份代码是「骨架/工程代码」，若想在其中直接跑 SITL，需在仓库旁边具备 PX4-1.16、gz_overrides/worlds、px4_msgs 等（见 README「依赖」）。

### 9.4 日常提交（下次直接）
```bash
cd /home/caolihao/drone_package
git add -A
git commit -m "描述"
git push origin main        # remote 已配好; 走 ~/.ssh/id_ed25519
```
- 本机 git 身份设为 `caolihao <1479888910@qq.com>`。
- SSH：用户家目录 `~/.ssh/id_ed25519`（本机，非仓库内）公钥已加入 lowtobeking 账户（`ssh -T git@github.com` 通）。
- 若某些文件须强制不进版本库：已由 `.gitignore` 覆盖；新增临时文件留意别 `git add .` 误压。


### 9.5 提交清单（至今 main 历史，越上越新）
```
589654f  fix(diag_monitor): 订阅 vehicle_local_position 用无后缀版兜底跨固件版本(v1.16命中无后缀)
58efac8  调试记忆追加§9: git仓库已收编并推到 github.com, 记录提交/SSH/身份
fbc344e  加回完善版 README(结构/依赖/复现入口)
a2d5106  初始整理: 纳入 Multi-UAV MPC 工程与 SITL 编排脚本/自建world/文档
cdeeaa7  Initial commit(GitHub 网页初始)
```
- 本次会话内新增的 git 提交共上行 5 个已全部 push 到 `lowtobeking/drone_package` main。
- 后续若不熟悉历史，`git log --oneline` 即查；换机重拉：`git clone git@github.com:lowtobeking/drone_package.git`。

