# Simulink MPC 编队验证 · 使用说明

> 不需要了解 ROS2、PX4、Ubuntu。只需要 MATLAB + Simulink + Aerospace Blockset。

---

## 这个代码是干什么的？

用 MATLAB/Simulink 模拟多架无人机一起飞行，验证控制算法是否稳定。

每架无人机都有一个"大脑"（MPC 控制器），负责计算下一步往哪飞。
模拟结束后自动给出**成绩单**：飞得好（PASS）、一般（REVIEW）、还是差（FAIL）。

---

## 第一步：打开 MATLAB，进入正确目录

```matlab
cd('C:\Users\Aaron\Desktop\5机仿真\report\simulink_formation\mpc_port')
```

---

## 第二步：选你想跑的场景，直接运行对应文件

### 按飞机数量选：

| 文件名前缀 | 飞机数量 | 队形 |
|---|---|---|
| `run_mpc_solo1_` | **1 架** | 单机 |
| `run_mpc_pair2_` | **2 架** | 前后纵列，间距 3m |
| `run_mpc_trio3_` | **3 架** | 等边三角形，间距约 5.2m |
| `run_mpc_cross5_` | **5 架** | 十字形，间距 3m |

### 按飞行方式选：

| 文件名后缀 | 飞行方式 | 说明 |
|---|---|---|
| `_hover` | **悬停** | 飞到指定高度，原地不动 |
| `_line` | **直线** | 向北飞 20 米后停下 |
| `_circle` | **圆周** | 绕半径 10 米的圆飞行 |

### 组合示例：

```matlab
% 想看 2架无人机 飞圆周？
run_mpc_pair2_circle

% 想看 5架无人机 飞直线？
run_mpc_cross5_line

% 想看 单架无人机 悬停？
run_mpc_solo1_hover

% 想看 3架无人机 飞圆周？
run_mpc_trio3_circle
```

直接在 MATLAB 命令窗口输入文件名回车即可，**不需要加任何参数**。

---

## 第三步：等待仿真完成

屏幕会显示进度：

```
=== MPC pair2 circle (2 机, T=65s) 仿真中...
  仿真完成 52.8s
  steady: form_err=0.000m  track_err=1.180m  alt_err=0.001m  min_sp=3.000m
  VERDICT: REVIEW
```

等待时间：
- 1 机 → 约 40 秒
- 2 机 → 约 55 秒
- 3 机 → 约 85 秒
- 5 机 → 约 120 秒

---

## 第四步：看结果

### 成绩单（文字）

| 成绩 | 含义 | 是否正常 |
|---|---|---|
| **PASS** | 飞得好，误差小 | ✅ 优秀 |
| **REVIEW** | 有点落后，但队形整齐 | ✅ 正常（直线/圆周固有滞后） |
| **FAIL** | 队形散了或出了安全问题 | ❌ 有问题 |

> **REVIEW 不是错误！** 直线和圆周飞行时，所有无人机会整体落后参考点约 1 米，
> 但彼此之间保持队形，这是算法特性，不是故障。

### 成绩表（汇总）

所有跑过的场景成绩都记录在：
```
mpc_port/results.csv
```
用 Excel 打开即可查看。

### 轨迹图（PNG）

每次仿真自动生成一张四格图：
```
report/figures/mpc_<队形>_<飞行方式>.png
```
例如：`mpc_pair2_circle.png`

图中四个格子分别显示：
- 左上：俯视飞行轨迹（圆周模式有灰色参考圆）
- 右上：编队误差随时间变化
- 左下：高度随时间变化
- 右下：最小间距随时间变化（多机时）/ 跟踪误差（单机时）

---

## 第五步：看单独场景的 3D 动画

仿真跑完后，想看某个场景的动画：

```matlab
% 格式：animate_mpc('队形', '飞行方式')

animate_mpc('solo1', 'hover')    % 单机悬停
animate_mpc('pair2', 'circle')   % 双机圆周
animate_mpc('trio3', 'line')     % 三机直线
animate_mpc('cross5', 'circle')  % 五机圆周
```

运行后会：
1. 弹出一个 3D 动画窗口，实时播放编队飞行过程（2倍速）
2. 同时把动画保存为 MP4 文件：`report/figures/mpc_<队形>_<飞行方式>_anim.mp4`

> 圆周模式的动画分两段：先播放**起飞爬升**（0~t_start，solo1/pair2/trio3 为 10s，cross5 为 30s），然后跳过收敛螺旋，直接显示**稳定圆周飞行**段。

---

## 全部 12 个场景一次性跑完

```matlab
% 在 MATLAB 中运行：
animate_all
```

会依次跑完所有 12 个场景，并生成对应 MP4 动画。总耗时约 15-18 分钟。

---

## 数字指标是什么意思？

```
form_err  = 编队误差（米）  ← 无人机之间的间距是否正确？目标 < 0.3m
track_err = 跟踪误差（米）  ← 飞行路径是否跟上了参考轨迹？目标 < 0.5m(PASS)
alt_err   = 高度误差（米）  ← 高度是否稳定在 -5m？目标 < 0.3m
min_sp    = 最小间距（米）  ← 无人机之间最近距离？须 > 1.5m（安全线）
```

---

## 12 个场景预期成绩一览

| 场景 | 成绩 | 关键指标 |
|---|---|---|
| solo1 悬停 | **PASS** | track_err = 0.000m |
| solo1 直线 | REVIEW | track_err = 0.561m |
| solo1 圆周 | REVIEW | track_err = 1.181m |
| pair2 悬停 | **PASS** | track_err = 0.000m，min_sp = 3.000m |
| pair2 直线 | REVIEW | track_err = 0.562m，min_sp = 3.000m |
| pair2 圆周 | REVIEW | track_err = 1.180m，min_sp = 3.000m |
| trio3 悬停 | **PASS** | track_err = 0.000m，min_sp = 5.196m |
| trio3 直线 | REVIEW | track_err = 0.565m，min_sp = 5.183m |
| trio3 圆周 | REVIEW | track_err = 1.185m，min_sp = 5.115m |
| cross5 悬停 | **PASS** | track_err = 0.000m，min_sp = 3.000m |
| cross5 直线 | **PASS** | track_err = 0.219m，min_sp = 2.992m |
| cross5 圆周 | **PASS** | track_err = 0.127m，min_sp = 2.991m |

> cross5（5机）之所以直线/圆周也是 PASS，是因为飞行速度更慢（1.0 m/s vs 1.5 m/s），
> 给控制器更充裕的时间响应。

---

## 如果想修改参数

所有参数集中在一个文件里：

```
mpc_port/formation_cfg.m
```

常用参数：

| 参数名 | 当前值 | 含义 |
|---|---|---|
| `cfg.lead_v` | 1.5 m/s | 飞行速度（直线/圆周；cross5 默认 1.0 m/s）|
| `cfg.lead_R` | 10 m | 圆周半径 |
| `cfg.lead_d` | 20 m | 直线飞行距离 |
| `cfg.target_alt` | -5.0 m | 飞行高度（NED坐标，负数=向上）|
| `cfg.d_safe` | 1.5 m | 无人机之间的安全距离 |
| `cfg.N` | 30 | MPC 预测步数（×0.05s = 1.5s 预测窗口）|

> **修改参数后直接重新运行 `run_mpc_*.m` 即可，不需要重启 MATLAB。**

---

## 新增：分布式"机载计算机"模型（更接近真机部署的仿真）

上面的模型里，所有无人机的控制算法写在**一个**程序块里统一计算。
真实部署时不是这样的——**每架无人机带一台自己的机载电脑，运行同一份
控制代码，只能收到通信范围内邻居的飞行状态**。

`swarm_distributed.slx`（**只有这一个文件**，换队形/机数是重跑脚本
覆盖重建它，不是另存一份）就是按这个真实结构搭的：打开模型能直观
看到 N 架飞机各有一个动力学模块 + 一个"机载计算机"模块（内容完全相同，
只有编号不同），每台机载计算机的输入线**只连着它的邻居**。MPC 和
一致性两种算法都装在里面，一个开关切换。

### 怎么跑（带实时 3D 动画）

```matlab
cd('C:\Users\Aaron\Desktop\5机仿真\report\simulink_formation\mpc_port')

run_distributed('cross5', 'circle')               % 5机十字编队飞圆周，MPC
run_distributed('cross5', 'circle', 'consensus')  % 同场景换一致性算法
run_distributed('grid9', 'line')                  % 9机方阵飞直线
run_distributed('pair2', 'hover')                 % 2机悬停
```

一条命令自动完成全部设置并弹出 3D 动画窗口，飞完后模型保持打开可以
自己翻看内部结构。

### 怎么出成绩单（不看动画、只要指标）

```matlab
scenario_run('pair2', 'hover', 'mpc_dist')        % 分布式MPC，出 PASS/REVIEW/FAIL
scenario_run('pair2', 'hover', 'consensus_dist')  % 分布式一致性
```

成绩记录在 `mpc_port/results_distributed*.csv`，轨迹图在
`report/figures/mpcd_*.png` / `consd_*.png`。

> 分布式模型和原模型的飞行结果**逐点完全一致**（已数值验证），
> 区别只在结构：一个"上帝视角统一算"，一个"每机各自算、只看邻居"。

---

## 常见问题

**Q：运行报错说找不到 `init.m`？**  
A：确认 MATLAB 当前目录在 `mpc_port/`，`init.m` 会自动从上级目录加载。

**Q：仿真结果和表格里的数字不一样？**  
A：正常。results.csv 是历史记录，重新跑会按最新参数计算。

**Q：`slprj/` 文件夹消失了？**  
A：没关系，它是编译缓存，下次运行会自动重新生成。

**Q：想只看某几个场景的动画？**  
A：直接运行 `animate_mpc('pair2', 'circle')` 即可，不用跑 `animate_all`。
