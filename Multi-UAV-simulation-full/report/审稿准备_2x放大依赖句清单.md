# 「2× 多机放大」依赖句清单

> ✅ **已全部落实（2026-09-01）**：零模型判定=**场景效应**（sim2 同机三臂，按机口径
> solo1 80.4 / null5 125.8 / cross5 125.1 m ⇒ null5/cross5=1.01；null5 vs solo1 δ=+1.00）。
> 本清单所列 EN/ZH 句子已全部改写进 `paper_clean.md` / `paper_clean_zh.md`（(v) 零模型小节
> 入 §6.2c 本体 + Fig.9；三版 PDF 已重建核验）。数字口径与复算入口：`report/m1_provenance.py`
> / `report/wf_significance.py`。
> 🔴 过程教训（详见记忆 uav-paper-draft 2026-09-01 节）：per-agent 的论文口径 =
> **逐架次机均值→跨架次中位**（§6.2c 自有定义），发表数字在此口径下全部逐位复现；
> 唯 p=0.55 不可复现，已更正为 p=0.69。曾一度误判"整组不可复现"并错改一轮，均已回正。

 —— 零模型实验出结果后一遍过

> 目的：五机独立单飞零模型（§7.2 已预告；`uav-paper-draft` 记忆里的 M1 实验）一旦出结果，
> 下列句子须**联动**修改。现在列清单，届时不靠 grep 现找、不漏。
> 判据回顾：零模型 per-agent p2p ≈100m ⇒ 放大是**场景效应**（共享风况/参考协议等），
> "多机放大"措辞全面降格；≈49m ⇒ 放大是**真多机效应**且来源仍开放（现措辞保留，补引实验）。

## 英文 `paper_clean.md`（行号为 2026-08-31 快照，届时以锚文本搜索为准）

| 行 | 锚文本 | 场景效应时怎么改 |
|---|---|---|
| 18 (Intro) | "roughly doubles in a five-vehicle formation, and is not removed by retuning" | 删或改为 "and appears larger in a five-vehicle scenario (an effect the null model of §6.2c attributes to the shared protocol, not to coupling)" |
| 28 (N1) | 检查 N1 段是否隐含多机放大（现主要靠 solo 49m，风险低） | 复核即可 |
| 157 (§5.5) | "The divergence does indeed roughly double … the doubling survives in full with every explicit inter-vehicle coupling" | 整段叙事重排：证伪链终点从"来源开放"改为"零模型归因" |
| 167 (§6.2c) | "roughly 2.1$\times$ the single-vehicle … no explicit coupling term accounts for that increase and we leave its origin open" | **核心改点**：把 "leave open" 替换为零模型结论 |
| 282 (§6.5) | "Five vehicles roughly double it, but not through the explicit coupling … implicit couplings alone already deliver 2.1$\times$" | "implicit couplings deliver 2.1×" 这句在场景效应结论下**不再成立**（隐式耦合≠共享协议），必须改写 |
| 396 (§7.3) | "a multi-vehicle deployment roughly doubles the resulting divergence for reasons we could not resolve" | 替换为归因结论；实践建议随之变（"多机不额外加剧"或"注意共享协议效应"） |
| §7.2 排队实验句 | "five-independent-solo null model … to separate scenario effects from genuinely multi-vehicle effects" | 从"排队"改为结果陈述 |
| 摘要 | 现版摘要**未提** 2×（只说 formation-cost 证伪）——复核无需动 | — |

## 中文 `paper_clean_zh.md`

| 行 | 锚 | 同步英文对应行 |
|---|---|---|
| 5 (摘要区) | "队形保持代价会放大" | 摘要证伪句，与 EN 摘要同判 |
| 96/98/100 (§5.5) | "放大器"/"翻倍完整存在" | ↔ EN 157 |
| 108 (§6.2c 引导) | "单机种子与多机放大" | ↔ EN 167 |
| 191 (§6.2c) | "全部显式机间耦合后五机就已放大" | ↔ EN 167/282 |
| 315 (§7 区) | "队形保持代价…放大的原因" | ↔ EN 396 附近 |
| §7.2 排队实验句 | "五机独立单飞的零模型" | ↔ EN 同句 |

## 别忘了的连带物

- **Table II-b 标题/读法**（§6.2c 表）：'vs. seed (per-agent)' 列的 "≈2.1×" 解释行
- **fig5 caption**（5 机发散图）：若归因场景效应，caption 里 "cross-vehicle divergence" 措辞
- **记忆**（`uav-paper-draft.md` / `rtk-outdoor-flight.md`）里的 "2× 放大" 表述
- 若结论=场景效应：**标题不受影响**（标题未提放大），但 §7.3 实践含义第二句要改

## 实验规格备忘（照 §7.2 预告执行）

5×solo1，**完全独立**（各自 leader、无共享世界系/碰撞项/编队项），风扰协议与
§6.2b 完全一致（2N、wind window 同窗口），n=5，统计口径 = per-agent p2p
（`tools/repeat_stats.py --wind-window 110 258`）。与现有 solo1 n=5（median 49m）
和 cross5 wf=0 n=5（per-agent median 102m）三方对比。
